# better-ssh
Better SSH - ssh config [/etc/ssh/sshd_config] for best security

> [!WARNING]
> You need enter ssh key to ~/.ssh/authorized_keys

# Quick Start
```bash
bash <(wget -qO- https://raw.githubusercontent.com/rediskazavr/better-ssh/refs/heads/main/ssh.sh)
```